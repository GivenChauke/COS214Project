#ifndef THICKBASEPIZZA_H
#define THICKBASEPIZZA_H

#include "PizzaBase.h"

/**
 * @brief The ThickBasePizza class represents a pizza with a thick crust base, which is a specific type of PizzaBase.
 */
class ThickBasePizza : public PizzaBase {
public:
    /**
     * @brief Constructor for ThickBasePizza to set its name and cost.
     */
    ThickBasePizza();
};

#endif
